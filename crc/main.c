#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main(int argc, char *argv[]) {
//argc = Anzahl der Argumente
//argv = Argumente
    if(argc < 4) {
        perror("zu wenig Argumente");
        return -1;
    }

    if (strcmp)
}